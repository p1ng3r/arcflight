import { VOYAGE_ENCOUNTER_LIFECYCLE_STATES as LIFE, VOYAGE_ROUND_PHASES as PHASES, VOYAGE_PENDING_CHECK_STATUSES as STATUSES } from "./constants.js";
import { clonePlainData, isPlainObject } from "./defaults.js";
import { validateVoyageEncounterState } from "./validation.js";
import { validateVoyageEncounterPendingChecks } from "./pending-checks.js";
import { deduplicateVoyageResolutionIssues } from "./resolution-order.js";
const error = (code, path, message) => ({ code, path, message, severity: "error" });
const fail = (errors, warnings = []) => ({ ok: false, nextState: null, events: [], errors: deduplicateVoyageResolutionIssues(errors), warnings: deduplicateVoyageResolutionIssues(warnings) });
function exact(value, fields) { try { if (!isPlainObject(value)) return false; const keys = Reflect.ownKeys(value); return keys.length === fields.length && fields.every((field) => { const d = Object.getOwnPropertyDescriptor(value, field); return d && Object.hasOwn(d, "value"); }) && keys.every((key) => typeof key === "string" && fields.includes(key)); } catch { return false; } }
function ordinaryEmptyArray(value) { try { if (!Array.isArray(value) || Object.getPrototypeOf(value) !== Array.prototype || value.length !== 0) return false; return Reflect.ownKeys(value).length === 1 && Object.getOwnPropertyDescriptor(value, "length")?.value === 0; } catch { return false; } }
function validInput(value) { const fields = ["ok", "status", "pendingCheckId", "sequence", "sourceKind", "sourceUuid", "statisticSlug", "dc", "rollMode", "result", "errors", "warnings"]; if (!exact(value, fields) || value.ok !== true || value.status !== "rolled" || value.sourceKind !== "character" || typeof value.pendingCheckId !== "string" || typeof value.sourceUuid !== "string" || typeof value.statisticSlug !== "string" || !Number.isSafeInteger(value.sequence) || value.sequence < 0 || !Number.isSafeInteger(value.dc) || value.dc < 0 || !["public", "blind"].includes(value.rollMode) || !ordinaryEmptyArray(value.errors) || !ordinaryEmptyArray(value.warnings)) return false; const r = value.result, slugs = ["critical-failure", "failure", "success", "critical-success"]; return exact(r, ["total", "degreeOfSuccess", "degreeOfSuccessSlug"]) && Number.isFinite(r.total) && Number.isSafeInteger(r.degreeOfSuccess) && r.degreeOfSuccess >= 0 && r.degreeOfSuccess <= 3 && r.degreeOfSuccessSlug === slugs[r.degreeOfSuccess]; }
export function applyVoyageEncounterPendingCheckResult(state, executionResult) { try {
  const structural = validateVoyageEncounterState(state); if (!structural.valid) return fail(structural.errors, structural.warnings);
  const authoritative = validateVoyageEncounterPendingChecks(state); const warnings = [...structural.warnings, ...authoritative.warnings]; if (!authoritative.valid) return fail(authoritative.errors, warnings);
  if (state.lifecycleState !== LIFE.ACTIVE || state.phase !== PHASES.RESOLUTION || !validInput(executionResult)) return fail([error("invalid-pending-check-result-application", "executionResult", "Result application requirements were not met.")], warnings);
  let matchedIndex = -1;
  for (let index = 0; index < state.pendingChecks.length; index += 1) { if (!Object.hasOwn(state.pendingChecks, index)) continue; const record = state.pendingChecks[index]; let statisticMatches = false; if (Array.isArray(record?.statisticOptions)) for (let optionIndex = 0; optionIndex < record.statisticOptions.length; optionIndex += 1) if (Object.hasOwn(record.statisticOptions, optionIndex) && record.statisticOptions[optionIndex] === executionResult.statisticSlug) statisticMatches = true;
    if (record?.pendingCheckId === executionResult.pendingCheckId && record.sequence === executionResult.sequence && record.source?.kind === executionResult.sourceKind && record.source?.uuid === executionResult.sourceUuid && statisticMatches && record.dcSource?.value === executionResult.dc && (record.secrecy === "secret" ? "blind" : "public") === executionResult.rollMode) { if (matchedIndex !== -1) return fail([error("pending-check-result-mismatch", "executionResult", "Result does not match one pending check.")], warnings); matchedIndex = index; }
  }
  if (matchedIndex === -1 || state.pendingChecks[matchedIndex].status !== STATUSES.PENDING || state.pendingChecks[matchedIndex].result !== null) return fail([error("pending-check-result-mismatch", "executionResult", "Result does not match one pending check.")], warnings);
  const candidate = clonePlainData(state); const target = candidate.pendingChecks[matchedIndex]; target.status = STATUSES.RESOLVED; target.result = { total: executionResult.result.total, degreeOfSuccess: executionResult.result.degreeOfSuccess, degreeOfSuccessSlug: executionResult.result.degreeOfSuccessSlug, statisticSlug: executionResult.statisticSlug, dc: executionResult.dc, rollMode: executionResult.rollMode }; candidate.revision = state.revision + 1;
  const final = validateVoyageEncounterState(candidate), pending = validateVoyageEncounterPendingChecks(candidate); warnings.push(...final.warnings, ...pending.warnings); if (!final.valid || !pending.valid) return fail([...final.errors, ...pending.errors], warnings);
  let ownCount = 0, resolved = 0; for (let index = 0; index < candidate.pendingChecks.length; index += 1) if (Object.hasOwn(candidate.pendingChecks, index)) { ownCount += 1; if (candidate.pendingChecks[index].status === STATUSES.RESOLVED) resolved += 1; } const remaining = ownCount - resolved;
  return { ok: true, nextState: candidate, events: [{ type: "voyage.pending-check-resolved", encounterId: candidate.encounterId, lifecycleState: candidate.lifecycleState, roundNumber: candidate.roundNumber, phase: candidate.phase, pendingCheckId: target.pendingCheckId, sequence: target.sequence, stationId: target.stationId, actionId: target.actionId, resolvedCheckCount: resolved, remainingCheckCount: remaining, allChecksResolved: remaining === 0, previousRevision: state.revision, revision: candidate.revision }], errors: [], warnings: deduplicateVoyageResolutionIssues(warnings) };
} catch { return fail([error("pending-check-result-application-failed", "executionResult", "Result application could not be completed safely.")]); } }
