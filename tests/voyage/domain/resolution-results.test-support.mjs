import { createVoyageEncounterState } from "../../../scripts/voyage/domain/state.js";
import { applyVoyageEncounterPendingCheckPreparation } from "../../../scripts/voyage/domain/pending-checks.js";
export function state() { const value = createVoyageEncounterState({ encounterId: "event", definitionId: "definition", primaryShip: { id: "ship" } }); value.lifecycleState = "active"; value.currentStage = { stageId: "stage" }; value.roundNumber = 1; value.phase = "resolution"; value.availableStations = [{ stationId: "captain", actions: [{ actionId: "check", resolutionPriority: 1, check: { source: { kind: "character", uuid: "Actor.a" }, statisticOptions: ["athletics"], dcSource: { kind: "fixed", value: 20 }, secrecy: "public" } }] }]; value.selections = { captain: { stationId: "captain", actionId: "check" } }; return applyVoyageEncounterPendingCheckPreparation(value, { pendingCheckIds: [{ sequence: 0, pendingCheckId: "check-1" }] }).nextState; }
export function rolled() { return { ok: true, status: "rolled", pendingCheckId: "check-1", sequence: 0, sourceKind: "character", sourceUuid: "Actor.a", statisticSlug: "athletics", dc: 20, rollMode: "public", result: { total: 24, degreeOfSuccess: 2, degreeOfSuccessSlug: "success" }, errors: [], warnings: [] }; }
